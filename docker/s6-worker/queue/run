#!/command/with-contenv bash
# S6 long-run service for Laravel Horizon queue manager
# Replaces raw queue:work — Horizon provides supervision, metrics, and dashboard.
# S6 supervises this — auto-restarts on exit.

cd /var/www/html

exec php artisan horizon
