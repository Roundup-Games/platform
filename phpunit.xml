<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <php>
        <ini name="memory_limit" value="1024M"/>
        <!-- Test-only key — not used in production. Generate your own with: php artisan key:generate -->
        <env name="APP_KEY" value="base64:XqyYW58m3W8Ag4bGDOmhrzAtQcjf8j7PxXLOFO9xHqM="/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="roundup_games_test"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Disable auto-share-link generation in tests so it does not pollute
             fixture state. Tests that exercise the auto-link explicitly opt in
             via config(['share.auto_generate_on_create' => true]). -->
        <env name="SHARE_AUTO_GENERATE_ON_CREATE" value="false"/>
        <!-- Disable auto-follow-on-join for the same reason. Tests that
             exercise auto-follow explicitly opt in. -->
        <env name="COMMUNITY_AUTO_FOLLOW_ON_JOIN" value="false"/>
    </php>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Filament/Resources</directory>
        </exclude>
    </source>
</phpunit>
